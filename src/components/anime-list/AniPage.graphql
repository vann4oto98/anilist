query GetAnimes($page: Int, $perPage: Int) {
  Page(page: $page, perPage: $perPage) {
    media {
      id
      title {
        english
        romaji
      }
      coverImage {
        large
      }
    }
  }
}
